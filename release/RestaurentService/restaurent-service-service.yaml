apiVersion: v1
kind: Service
metadata:
  name: restaurent-service
  namespace: smartdelivery
spec:
  selector:
    app: restaurent-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080