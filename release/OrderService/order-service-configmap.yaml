apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  namespace: smartdelivery
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "AllowedHosts": "*",
      "ConnectionStrings": {
        "OrderDatabase": "__ORDER_DB_CONNECTION__"
      },
      "ServiceName": "OrderService"
    }
