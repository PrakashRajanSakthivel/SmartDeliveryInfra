# -----------------------------------------------------------------------
# Elasticsearch — SmartDelivery
#
# NOT a Helm chart. Deployed via kubectl apply (raw manifest).
#
# Manifest location: release/logging/elasticsearch.yaml
# Namespace:         release/logging/namespace.yaml
#
# Apply:
#   kubectl apply -f release/logging/namespace.yaml
#   kubectl apply -f release/logging/elasticsearch.yaml
#
# Versions: elasticsearch:8.15.0
# Resources: requests cpu=500m/memory=1Gi  limits cpu=1000m/memory=1.5Gi
# Storage:   Hetzner Volume (PVC: elasticsearch-data, PV: elasticsearch-hetzner-pv)
#            hostPath: /mnt/es-pv on sd-master, reclaimPolicy: Retain
# PV/PVC:    release/logging/elasticsearch-pv.yaml
#            release/logging/elasticsearch-pvc.yaml
# -----------------------------------------------------------------------