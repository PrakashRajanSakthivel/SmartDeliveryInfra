# -----------------------------------------------------------------------
# Kibana — SmartDelivery
#
# NOT a Helm chart. Deployed via kubectl apply (raw manifest).
#
# Manifest location: release/logging/kibana.yaml
# Namespace:         release/logging/namespace.yaml
#
# Apply:
#   kubectl apply -f release/logging/namespace.yaml
#   kubectl apply -f release/logging/kibana.yaml
#
# Versions: kibana:8.15.0
# Resources: requests cpu=500m/memory=512Mi  limits cpu=1000m/memory=1Gi
# Service:   NodePort :30601
# -----------------------------------------------------------------------